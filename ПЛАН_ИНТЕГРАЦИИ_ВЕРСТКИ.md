# План интеграции верстки v-massaj на Django сайт

## 📋 Обзор

В папке `v-massaj` находится готовая верстка для основных страниц сайта:
- **index.html** - Главная страница
- **uslugi.html, uslugi2.html, uslugi3.html, uslugi4.html** - Страницы услуг (категории)
- **uslugi2.html** - Пример страницы конкретной услуги (детальная страница)
- **mastera.html, mastera2.html** - Страницы мастеров
- **akcii.html** - Страница акций
- **kompleks.html** - Страница комплексов
- **kontakty.html** - Страница контактов

Статические файлы:
- `css/` - стили (bootstrap, main.css, owl.carousel, glightbox)
- `js/` - скрипты (bootstrap, main.js, owl.carousel, glightbox, wow.js)
- `i/` - изображения (72 файла)
- `f/` - шрифты (NTSomic)
- Видео файлы (video-mob.mp4, video-pk.mp4 и т.д.)

---

## 🎯 Этап 1: Подготовка статических файлов

### 1.1. Создание структуры статических файлов

**Действия:**
1. Создать папку `mysite/static/` (если не существует)
2. Скопировать файлы из `v-massaj/` в `mysite/static/`:
   ```
   mysite/static/
   ├── css/
   │   ├── bootstrap-5.0.0-beta1.min.css
   │   ├── main.css
   │   ├── owl.carousel.min.css
   │   └── glightbox.min.css
   ├── js/
   │   ├── bootstrap.bundle-5.0.0-beta1.min.js
   │   ├── main.js
   │   ├── owl.carousel.min.js
   │   ├── glightbox.min.js
   │   ├── imagesloaded.min.js
   │   └── wow.min.js
   ├── images/  (переименовать из i/)
   │   ├── logo/
   │   │   └── logo.svg
   │   └── [все остальные изображения]
   ├── fonts/  (переименовать из f/)
   │   └── [все шрифты .woff, .woff2]
   └── video/
       ├── video-mob.mp4
       ├── video-mob.webm
       ├── video-pk.mp4
       └── video-pk.webm
   ```

**Примечание:** Папку `i/` лучше переименовать в `images/` для ясности, `f/` в `fonts/`.

### 1.2. Настройка STATICFILES в settings

**Файл:** `mysite/mysite/settings/base.py`

Проверить, что `STATICFILES_DIRS` настроен правильно:
```python
STATICFILES_DIRS = [
    BASE_DIR / "static",  # Убедиться, что путь правильный
]
```

---

## 🎨 Этап 2: Обновление базового шаблона

### 2.1. Анализ структуры header и footer из верстки

**Файл:** `mysite/website/templates/website/base.html`

**Действия:**
1. Заменить текущий `<head>` на структуру из `v-massaj/index.html`:
   - Подключить CSS файлы через `{% static %}`:
     - `bootstrap-5.0.0-beta1.min.css`
     - `main.css`
     - `owl.carousel.min.css` (если используется)
     - `glightbox.min.css` (если используется)
   - Добавить favicon
   - Добавить мета-теги

2. Заменить `<header>` на версию из верстки:
   - Использовать структуру с `navbar-expand-xl`
   - Добавить логотип через `{% static 'images/logo/logo.svg' %}`
   - Интегрировать навигацию с Django URL:
     - `{% url 'website:home' %}` для Главной
     - `{% url 'website:services' %}` для Услуг
     - `{% url 'website:masters' %}` для Мастеров
     - `{% url 'website:promotions' %}` для Акций
     - `{% url 'website:bundles' %}` для Комплексов
     - `{% url 'website:contacts' %}` для Контактов
   - Добавить кнопку "Записаться онлайн" (можно оставить заглушку или подключить модальное окно)
   - Добавить телефон из настроек: `{{ settings.phone|default:"8 (8412) 39-34-33" }}`

3. Заменить `<footer>` на версию из верстки:
   - Добавить контакты из `settings`
   - Добавить социальные сети (если есть в настройках)
   - Добавить копирайт

4. Добавить preloader (опционально, можно оставить или убрать)

5. Подключить JS файлы в конце `<body>`:
   - `bootstrap.bundle-5.0.0-beta1.min.js`
   - `main.js`
   - `owl.carousel.min.js` (если используется)
   - `glightbox.min.js` (если используется)
   - `wow.min.js` (если используется)

**Блоки для шаблона:**
- `{% block extra_css %}` - для дополнительных стилей на страницах
- `{% block extra_js %}` - для дополнительных скриптов
- `{% block body_class %}` - для классов body (например, `class="page"` или `class="home"`)

---

## 📄 Этап 3: Интеграция страниц

### 3.1. Главная страница (index.html → home.html)

**Файл:** `mysite/website/templates/website/home.html`

**Действия:**
1. Взять структуру из `v-massaj/index.html`
2. Заменить статический контент на Django шаблоны:
   - Баннер с видео/изображением
   - Секция "Популярные услуги" - использовать `{% for item in top_items %}`
   - Секция "Акции" - использовать `{% for p in promotions %}`
   - Секция "Комплексы" - использовать `{% for b in popular_bundles %}`
   - Секция FAQ - использовать `{% for f in faq %}`
3. Обновить пути к изображениям на `{% static 'images/...' %}`
4. Интегрировать формы записи (если есть в верстке) с существующей логикой

**Проверить в views.py:**
- `home()` view уже передает нужные данные: `top_items`, `promotions`, `popular_bundles`, `faq`

### 3.2. Страница списка услуг (uslugi.html → services.html)

**Файл:** `mysite/website/templates/website/services.html`

**Действия:**
1. Взять структуру из `v-massaj/uslugi.html` или `uslugi2.html`
2. Заменить статический список услуг на:
   ```django
   {% for cat in categories %}
     <h2>{{ cat.name }}</h2>
     {% for s in cat.services.all %}
       {% if s.is_active %}
         <!-- Карточка услуги -->
       {% endif %}
     {% endfor %}
   {% endfor %}
   ```
3. Интегрировать форму записи с модальным окном YClients (уже есть в текущем `services.html`)
4. Обновить пути к изображениям

**Примечание:** В текущем `services.html` уже есть функционал записи через модальное окно. Нужно сохранить его, но обернуть в новую верстку.

### 3.3. Страница детальной услуги (uslugi2.html → service_detail.html)

**Файл:** `mysite/website/templates/website/service_detail.html`

**Действия:**
1. Взять структуру из `v-massaj/uslugi2.html` (пример страницы конкретной услуги)
2. Заменить статический контент на:
   - `{{ service.name }}` - название
   - `{{ service.description|linebreaks }}` - описание
   - Изображение услуги (если есть в модели)
3. **ВАЖНО:** Сохранить существующую форму бронирования из текущего `service_detail.html`:
   - Выбор варианта услуги
   - Выбор мастера
   - Выбор даты (flatpickr)
   - Выбор времени
   - Модальное окно с контактами
   - JavaScript логику для работы с API
4. Адаптировать стили формы под новую верстку

### 3.4. Страница мастеров (mastera.html → masters.html)

**Файл:** `mysite/website/templates/website/masters.html`

**Действия:**
1. Взять структуру из `v-massaj/mastera.html`
2. Заменить статический список мастеров на:
   ```django
   {% for master in masters %}
     <!-- Карточка мастера -->
     <img src="{{ master.photo.url }}" alt="{{ master.name }}">
     <h3>{{ master.name }}</h3>
     {% if master.specialization %}<p>{{ master.specialization }}</p>{% endif %}
     <!-- Список услуг мастера -->
   {% endfor %}
   ```
3. Обновить пути к изображениям

### 3.5. Страница акций (akcii.html → promotions.html)

**Файл:** `mysite/website/templates/website/promotions.html`

**Действия:**
1. Взять структуру из `v-massaj/akcii.html`
2. Заменить статический список акций на:
   ```django
   {% for p in promotions %}
     <!-- Карточка акции -->
     <h3>{{ p.title }}</h3>
     {% if p.subtitle %}<p>{{ p.subtitle }}</p>{% endif %}
     {% if p.discount_percent %}<span>Скидка: {{ p.discount_percent }}%</span>{% endif %}
   {% endfor %}
   ```
3. Обновить пути к изображениям

### 3.6. Страница комплексов (kompleks.html → bundles.html)

**Файл:** `mysite/website/templates/website/bundles.html`

**Действия:**
1. Взять структуру из `v-massaj/kompleks.html`
2. Заменить статический список комплексов на:
   ```django
   {% for bundle_data in bundles %}
     {% with b=bundle_data.bundle items=bundle_data.items %}
       <h3>{{ b.name }}</h3>
       <p>Цена: {{ bundle_data.price|default:bundle_data.min_price }} ₽</p>
       <ul>
         {% for item in items %}
           <li>{{ item.option.service.name }} - {{ item.option.duration_min }} мин</li>
         {% endfor %}
       </ul>
     {% endwith %}
   {% endfor %}
   ```
3. Обновить пути к изображениям

### 3.7. Страница контактов (kontakty.html → contacts.html)

**Файл:** `mysite/website/templates/website/contacts.html`

**Действия:**
1. Взять структуру из `v-massaj/kontakty.html`
2. Заменить статический контент на данные из `settings`:
   - Адрес: `{{ settings.address|default:"г. Пенза, ул. Пушкина, 45" }}`
   - Телефон: `{{ settings.phone|default:"8 (8412) 39-34-33" }}`
   - Время работы: `{{ settings.working_hours|default:"Ежедневно: с 10:00-21:00" }}`
   - Карта (если есть в верстке)
3. Обновить пути к изображениям

---

## 🔧 Этап 4: Адаптация форм и модальных окон

### 4.1. Модальное окно записи

**Действия:**
1. Если в верстке есть модальное окно записи (`#exampleModal`), интегрировать его в `base.html`
2. Подключить к кнопкам "Записаться онлайн" в header
3. Адаптировать под существующую логику записи (YClients API)

### 4.2. Формы на страницах

**Действия:**
1. Проверить все формы в верстке
2. Адаптировать под Django формы (если нужно)
3. Сохранить существующую функциональность форм бронирования

---

## 🎨 Этап 5: Стилизация и адаптивность

### 5.1. Проверка CSS

**Действия:**
1. Убедиться, что `main.css` подключен в `base.html`
2. Проверить, что все классы из верстки работают
3. При необходимости добавить дополнительные стили в `{% block extra_css %}`

### 5.2. Адаптивность

**Действия:**
1. Проверить, что верстка адаптивна (Bootstrap 5 уже используется)
2. Протестировать на разных разрешениях
3. Убедиться, что мобильное меню работает

---

## 📝 Этап 6: Обновление путей и ссылок

### 6.1. Замена относительных путей

**Действия:**
Во всех шаблонах заменить:
- `href="index.html"` → `href="{% url 'website:home' %}"`
- `href="uslugi.html"` → `href="{% url 'website:services' %}"`
- `src="i/..."` → `src="{% static 'images/...' %}"`
- `href="css/..."` → `href="{% static 'css/...' %}"`
- `src="js/..."` → `src="{% static 'js/...' %}"`

### 6.2. Использование Django template tags

**Действия:**
1. Добавить `{% load static %}` в начало каждого шаблона
2. Использовать `{% static %}` для всех статических файлов
3. Использовать `{% url %}` для всех внутренних ссылок

---

## ✅ Этап 7: Тестирование

### 7.1. Функциональное тестирование

**Проверить:**
- [ ] Все страницы открываются без ошибок
- [ ] Навигация работает корректно
- [ ] Формы бронирования работают
- [ ] Модальные окна открываются/закрываются
- [ ] Изображения загружаются
- [ ] Шрифты отображаются правильно
- [ ] JavaScript работает (карусели, модальные окна, анимации)

### 7.2. Визуальное тестирование

**Проверить:**
- [ ] Верстка соответствует дизайну из v-massaj
- [ ] Адаптивность на мобильных устройствах
- [ ] Все элементы на месте
- [ ] Нет "битых" изображений

### 7.3. Производительность

**Проверить:**
- [ ] Статические файлы кешируются
- [ ] Изображения оптимизированы
- [ ] JavaScript не блокирует загрузку

---

## 📦 Этап 8: Оптимизация (опционально)

### 8.1. Сборка статических файлов

**Действия:**
1. Запустить `python manage.py collectstatic` для production
2. Проверить, что все файлы собраны в `STATIC_ROOT`

### 8.2. Минификация (если нужно)

**Действия:**
1. CSS и JS файлы уже минифицированы (судя по названиям)
2. При необходимости можно добавить django-compressor

---

## 🚀 Порядок выполнения (рекомендуемый)

1. **Этап 1** - Подготовка статических файлов (скопировать файлы)
2. **Этап 2** - Обновление базового шаблона (base.html)
3. **Этап 3.1** - Главная страница (home.html)
4. **Этап 3.2** - Список услуг (services.html)
5. **Этап 3.3** - Детальная страница услуги (service_detail.html) ⚠️ **ВАЖНО:** Сохранить функционал бронирования
6. **Этап 3.4-3.7** - Остальные страницы
7. **Этап 4** - Адаптация форм
8. **Этап 5** - Стилизация
9. **Этап 6** - Обновление путей
10. **Этап 7** - Тестирование

---

## ⚠️ Важные замечания

1. **Сохранить функционал бронирования:** В `service_detail.html` уже есть рабочая форма бронирования с API. Нужно сохранить её полностью, только обернуть в новую верстку.

2. **Модальное окно YClients:** В `services.html` уже есть модальное окно для записи через YClients. Сохранить его функционал.

3. **Пути к статическим файлам:** Всегда использовать `{% static %}` вместо относительных путей.

4. **Данные из БД:** Использовать данные из моделей Django, а не статический контент из HTML.

5. **Тестирование после каждого этапа:** После интеграции каждой страницы проверять, что всё работает.

---

## 📚 Дополнительные ресурсы

- Документация Django: https://docs.djangoproject.com/en/stable/ref/templates/builtins/#std:templatetag-static
- Bootstrap 5 документация: https://getbootstrap.com/docs/5.0/
- Owl Carousel: https://owlcarousel2.github.io/OwlCarousel2/

---

**Дата создания:** 2025-01-XX  
**Версия:** 1.0

