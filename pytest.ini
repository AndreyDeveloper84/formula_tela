[pytest]
DJANGO_SETTINGS_MODULE = mysite.settings
pythonpath = mysite
testpaths = mysite/tests
addopts = -q -m "not live"
markers =
    live: тесты с реальным YClients API — запускать вне CI (pytest mysite/tests/test_booking_live.py -v -s)
