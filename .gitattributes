# Нормализация line endings: все текстовые файлы хранятся и чекаутятся в LF.
# Windows-клиенты, коммитящие файлы с CRLF, при следующем `git add` получают
# автоматический convert в LF. Бинарные файлы определяются авто-детектом git.
* text=auto eol=lf
