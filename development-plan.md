# 🎯 План разработки: Массажный салон → Полноценный продакшн

## 📊 Текущее состояние проекта (что уже есть)

✅ **Django-архитектура:**
- 3 приложения: `booking`, `services_app`, `website`
- Модели для услуг, мастеров, акций, комплексов
- Админка с кастомным импортом CSV
- Базовые views и шаблоны

✅ **Начальная интеграция YClients:**
- Поля `yclients_company_id`, `yclients_service_id` в моделях
- JavaScript-заглушка для виджета в `base.html`

✅ **CI/CD каркас:**
- GitHub Actions workflows для staging и production
- Деплой через SSH
- Health check endpoint

⚠️ **Что нужно доделать:**
- Полноценная интеграция с YClients API
- Надёжный CI/CD pipeline
- Современная верстка вместо базовых стилей

---

## 🗺️ Пошаговый план (разбит на спринты)

### **ФАЗА 1: Фундамент (1-2 недели)**

#### **Спринт 1.1: Настройка окружения и CI/CD базы**

**Что делаем:**
1. **Локальное окружение:**
   - Создаём `docker-compose.yml` для разработки (Django + PostgreSQL + Redis)
   - Настраиваем `.env.example` с описанием всех переменных
   - Пишем `Dockerfile` для приложения

2. **Базовый CI/CD:**
   - Улучшаем `.github/workflows/ci.yml`:
     - Добавляем линтеры (flake8, isort, black)
     - Настраиваем pytest с покрытием
     - Проверяем миграции на конфликты
   - Фиксим deploy workflows (сейчас там есть баги)

**Зачем это нужно:**
- Docker = одинаковое окружение у всех разработчиков
- CI проверяет код автоматически → меньше багов на продакшне
- Линтеры = читаемый код

**Пример задачи для тебя:**
```yaml
# .github/workflows/ci.yml (улучшенная версия)
# Будет предоставлен полный пример с пояснениями
```

---

#### **Спринт 1.2: Подготовка базы данных**

**Что делаем:**
1. Переводим на PostgreSQL (если ещё на SQLite)
2. Настраиваем миграции для безопасного деплоя:
   - Проверяем все `migrations/` на конфликты
   - Делаем `squash` старых миграций (опционально)
3. Настраиваем резервное копирование БД

**Зачем:**
- SQLite не для продакшна (нет concurrent writes)
- Правильные миграции = нет простоев при обновлении

---

### **ФАЗА 2: Интеграция с YClients (2-3 недели)**

#### **Спринт 2.1: YClients API — изучение и первый запрос**

**Что делаем:**
1. **Изучаем документацию YClients:**
   - Регистрируемся, получаем API-ключ
   - Тестируем базовые эндпоинты в Postman/Insomnia:
     - `GET /services` (список услуг)
     - `GET /staff` (список мастеров)
     - `POST /book_record` (создание записи)

2. **Создаём Django-утилиту для работы с API:**
   ```python
   # services_app/yclients_api.py
   import requests
   from django.conf import settings
   
   class YClientsAPI:
       def __init__(self):
           self.base_url = "https://api.yclients.com/api/v1"
           self.headers = {
               "Authorization": f"Bearer {settings.YCLIENTS_API_KEY}",
               "Accept": "application/vnd.yclients.v2+json"
           }
       
       def get_services(self):
           # Получить список услуг
           ...
   ```

**Зачем:**
- Тестируем API вручную → понимаем, как оно работает
- Отдельный модуль = чистый код, легко тестировать

---

#### **Спринт 2.2: Синхронизация данных**

**Что делаем:**
1. **Django management команда для импорта:**
   ```bash
   python manage.py sync_yclients_services
   ```
   - Загружает услуги из YClients
   - Обновляет `yclients_service_id` в моделях
   - Логирует изменения

2. **Настраиваем Celery (опционально) для фоновых задач:**
   - Периодическая синхронизация (раз в сутки)
   - Обработка вебхуков YClients

**Зачем:**
- Автоматическая синхронизация = актуальные цены и услуги
- Celery = не блокируем основной поток

---

#### **Спринт 2.3: Онлайн-запись через виджет**

**Что делаем:**
1. **Доделываем JavaScript-интеграцию:**
   - Убираем заглушки из `base.html`
   - Передаём правильные `service_id`, `staff_id` в виджет
   - Тестируем на staging

2. **Альтернатива: прямая запись через API (продвинутый вариант):**
   - Форма на сайте → POST в YClients напрямую
   - Кастомный дизайн вместо iframe

**Зачем:**
- Виджет = быстро и просто
- Прямой API = полный контроль над UX

---

### **ФАЗА 3: Современная верстка (2-3 недели)**

#### **Спринт 3.1: Подготовка дизайна**

**Что делаем:**
1. **Анализируем готовый дизайн:**
   - Figma/PSD → экспортируем ассеты (картинки, иконки)
   - Определяем компоненты (кнопки, карточки, формы)
   - Выбираем подход:
     - **Вариант А:** Tailwind CSS (быстро, уже в проекте)
     - **Вариант Б:** Bootstrap + кастомный CSS
     - **Вариант В:** Чистый CSS/SCSS

2. **Настраиваем сборку стилей:**
   - Если Tailwind: настраиваем `tailwind.config.js`, подключаем PostCSS
   - Если SCSS: настраиваем компиляцию в Django pipeline

**Зачем:**
- Единый дизайн-система = профессиональный вид
- Tailwind/Bootstrap = быстрая разработка

---

#### **Спринт 3.2: Вёрстка страниц**

**Порядок работы (от главной к второстепенным):**

1. **Главная страница (`home.html`):**
   - Герой-секция с призывом к действию
   - Блок популярных услуг (карточки)
   - Акции, отзывы, FAQ

2. **Каталог услуг (`services.html`):**
   - Фильтры по категориям
   - Карточки услуг с выбором варианта
   - Кнопка "Записаться" → виджет YClients

3. **Страницы акций, мастеров, контактов:**
   - Единый стиль карточек
   - Адаптивная вёрстка (mobile-first)

4. **Админка (опционально):**
   - Кастомизируем Django Admin через `admin.py` и CSS

**Технические моменты:**
```html
<!-- Пример: карточка услуги с Tailwind -->
<div class="bg-white rounded-lg shadow-md p-6 hover:shadow-xl transition">
  <h3 class="text-xl font-bold mb-2">{{ service.name }}</h3>
  <p class="text-gray-600">{{ service.short }}</p>
  <div class="mt-4 flex justify-between items-center">
    <span class="text-2xl font-bold">{{ service.price_from }} ₽</span>
    <button class="btn btn-primary" data-booking data-service-id="{{ service.yclients_service_id }}">
      Записаться
    </button>
  </div>
</div>
```

---

#### **Спринт 3.3: Мобильная версия и оптимизация**

**Что делаем:**
1. **Адаптивность:**
   - Проверяем все страницы на телефоне (Chrome DevTools)
   - Исправляем баги с overflow, мелкими шрифтами
   - Hamburger-меню для навигации

2. **Оптимизация загрузки:**
   - Сжимаем изображения (TinyPNG, WebP формат)
   - Ленивая загрузка картинок (`loading="lazy"`)
   - Минификация CSS/JS в production

3. **SEO-базис:**
   - Мета-теги (title, description) через контекст
   - Open Graph для соцсетей
   - Sitemap и robots.txt

---

### **ФАЗА 4: Продвинутый CI/CD (1-2 недели)**

#### **Спринт 4.1: Автоматизация тестирования**

**Что добавляем в CI:**

```yaml
# .github/workflows/ci.yml
jobs:
  test:
    steps:
      # ... setup ...
      
      - name: Run linters
        run: |
          flake8 mysite --max-line-length=120
          isort --check-only mysite
          black --check mysite
      
      - name: Run tests with coverage
        run: |
          pytest --cov=mysite --cov-report=xml
      
      - name: Check migrations
        run: |
          python manage.py makemigrations --check --dry-run
      
      - name: Security check
        run: |
          pip install safety
          safety check
```

**Зачем:**
- Линтеры отлавливают опечатки и плохой код
- Тесты = уверенность, что ничего не сломалось
- `--check` миграций = разработчик не забыл закоммитить миграцию

---

#### **Спринт 4.2: Docker-образы и Registry**

**Что делаем:**
1. **Создаём production Dockerfile:**
   ```dockerfile
   FROM python:3.12-slim
   ENV PYTHONUNBUFFERED=1
   WORKDIR /app
   
   # Зависимости
   COPY requirements.txt .
   RUN pip install --no-cache-dir -r requirements.txt
   
   # Код приложения
   COPY mysite/ ./mysite/
   
   # Entrypoint
   CMD ["gunicorn", "mysite.wsgi:application", "--bind", "0.0.0.0:8000"]
   ```

2. **Настраиваем GitHub Container Registry:**
   - Пушим образы в `ghcr.io/твой-юзернейм/formula-tela:latest`
   - В CI добавляем сборку образа после тестов

**Зачем:**
- Образ = одинаковое окружение на staging и production
- Registry = централизованное хранение версий

---

#### **Спринт 4.3: Безопасный деплой с откатами**

**Улучшаем deploy workflow:**

1. **Blue/Green деплой (упрощённый):**
   ```bash
   # На сервере:
   # 1. Запускаем новый контейнер (green)
   docker-compose up -d web_green
   
   # 2. Проверяем health check
   curl http://localhost:8001/healthz
   
   # 3. Переключаем nginx на новый порт
   # 4. Останавливаем старый (blue)
   ```

2. **Миграции в отдельном шаге:**
   ```yaml
   - name: Run migrations
     run: |
       ssh user@server "cd /app && docker-compose exec web python manage.py migrate --noinput"
   
   - name: Wait for health check
     run: |
       for i in {1..30}; do
         curl -f https://formulatela58.ru/healthz && break
         sleep 2
       done
   ```

3. **Rollback-процедура:**
   ```bash
   # Откат на предыдущий Docker-образ
   docker pull ghcr.io/твой-юзернейм/formula-tela:previous
   docker-compose up -d
   
   # Откат миграций (если нужно)
   python manage.py migrate services_app 0015  # номер предыдущей миграции
   ```

**Зачем:**
- Health check = не пускаем битый код в продакшн
- Rollback = быстро восстанавливаемся после ошибки

---

### **ФАЗА 5: Финальная полировка (1 неделя)**

#### **Чеклист перед запуском:**

**Безопасность:**
- [ ] `DEBUG = False` в продакшне
- [ ] Секреты в environment variables (не в коде!)
- [ ] HTTPS включён (сертификат Let's Encrypt)
- [ ] CSP headers настроены

**Производительность:**
- [ ] Включён `django.middleware.cache` (опционально)
- [ ] Статика отдаётся через nginx (не Django)
- [ ] База данных: индексы на часто запрашиваемые поля

**Мониторинг:**
- [ ] Sentry для отслеживания ошибок
- [ ] Логи пишутся в файл (ротация через `logrotate`)
- [ ] Uptime-мониторинг (UptimeRobot, Pingdom)

**Документация:**
- [ ] README с инструкцией по запуску
- [ ] `.env.example` с описанием переменных
- [ ] Схема архитектуры (опционально)

---

## 🎓 Процесс обучения

Каждый спринт включает:
1. **Объяснение теории** простым языком
2. **Готовый код** с комментариями
3. **Практические задачи** для закрепления
4. **Ответы на вопросы** в удобном темпе

---

## 📋 Приоритеты для старта

**Рекомендуемый порядок:**

1. **Спринты 1.1-1.2** — Docker и CI/CD (фундамент)
2. **Спринт 2.1** — Интеграция с YClients (критически важно для бизнеса)
3. **Спринты 3.1-3.2** — Верстка (визуальная составляющая)
4. **Спринты 4.1-4.3** — Продвинутый CI/CD (стабильность)
5. **Фаза 5** — Финальная полировка

---

## 📝 Следующие шаги

1. Выбери спринт для начала работы
2. Получи детальные инструкции и примеры кода
3. Реализуй функционал
4. Переходи к следующему спринту

**Удачи в разработке! 🚀**
