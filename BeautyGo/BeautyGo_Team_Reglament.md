
# 📄 Командный регламент разработки проекта BeautyGo

---

## 🔹 1. Git flow и ветвление

**Главные ветки:**
- `main` — продакшн, только стабильный код, защищённая ветка
- `dev` — общая ветка для слияния фич и тестирования
- `feature/*` — отдельные фичи (feature/auth, feature/profile)
- `bugfix/*` — багфиксы

**Правила:**
- Любая новая задача — новая ветка от `dev`
- После завершения: Pull Request в `dev`
- После тестирования на dev-сервере — PR из `dev` в `main`
- Обязательный **code review** для слияния в `dev` и `main`

---

## 🔹 2. Code style и форматирование

- Python (Django): `black`, `flake8`, `isort`
- JS/TS (React Native): `prettier`, `eslint`

**Форматирование автофиксируется через pre-commit хуки**

---

## 🔹 3. Названия коммитов

Формат:
```
[feature] регистрация через email
[fix] исправлен баг с отображением услуг
[refactor] вынесены стили в отдельный файл
```

---

## 🔹 4. CI/CD

- Деплой `main` → staging/prod (через Railway / Render)
- Настроить GitHub Actions:
  - запуск линтеров и тестов
  - деплой после PR в `main`

---

## 🔹 5. Работа с тасками

- Используем Trello / ClickUp / Notion:
  - каждая задача = карточка
  - карточка содержит: описание, чеклист, приоритет, исполнителя
- Статусы: `To do → In progress → Code Review → Done`

---

## 🔹 6. Согласование фич и API

- Все API-договорённости фиксируются в `api-spec.md`
- Перед началом работы над фичей — короткое обсуждение: кто что делает, какая структура
- Если API меняется — уведомить другого разработчика + обновить документацию

---

## 🔹 7. Документация

- Документы храним в Notion / GitHub Wiki
- Описываем:
  - Архитектуру
  - Базу данных
  - API
  - Флоу пользователей

---

## 🔹 8. Совещания

- Мини-планерка 2–3 раза в неделю (15 мин, синхронизация)
- Созвон по ключевым архитектурным решениям
