
# 📄 API-спецификация MVP

Формат: RESTful API  
Аутентификация: JWT (Bearer Token)

---

## 🔹 1. Аутентификация и авторизация

### POST `/api/auth/register/` — Регистрация
**Тело запроса:**
```json
{
  "email": "user@example.com",
  "phone": "+79990001122",
  "password": "string"
}
```

### POST `/api/auth/login/` — Вход
**Тело запроса:**
```json
{
  "email": "user@example.com",
  "password": "string"
}
```
**Ответ:**
```json
{
  "access": "jwt_token",
  "refresh": "refresh_token"
}
```

### POST `/api/auth/social-login/` — OAuth
**Тело запроса:**
```json
{
  "provider": "google",
  "access_token": "string"
}
```

---

## 🔹 2. Профиль специалиста

### GET `/api/profile/me/` — Мой профиль

### PATCH `/api/profile/me/` — Обновить профиль
```json
{
  "full_name": "Анна Иванова",
  "bio": "Опыт 10 лет",
  "experience": 10
}
```

---

## 🔹 3. Услуги

### GET `/api/services/` — Список услуг

### POST `/api/services/` — Создать услугу
```json
{
  "name": "Массаж лица",
  "price": 2500,
  "duration": 60,
  "description": "Улучшает лимфоток"
}
```

---

## 🔹 4. Расписание

### GET `/api/schedule/` — Мое расписание

### POST `/api/schedule/` — Добавить расписание
```json
{
  "weekday": 1,
  "start_time": "10:00",
  "end_time": "18:00"
}
```

---

## 🔹 5. Бронирование

### GET `/api/appointments/available/` — Свободные слоты
**Параметры:** `?service_id=1`

### POST `/api/appointments/` — Создать запись
```json
{
  "service_id": 1,
  "datetime": "2025-06-01T12:00:00"
}
```

---

## 🔹 6. Оплата

### POST `/api/payments/initiate/` — Начать оплату
```json
{
  "appointment_id": 10
}
```
**Ответ:** URL платёжной формы

### POST `/api/payments/webhook/` — Webhook от платёжной системы

---

## 🔹 7. Админка (только Django Admin)

- Подтверждение профилей
- Управление пользователями
- Просмотр записей и оплат

---

## 🔐 Авторизация

Все защищённые запросы требуют заголовок:
```
Authorization: Bearer <token>
```

---

## ❗ Обработка ошибок

Ошибки возвращаются в формате:
```json
{
  "detail": "Описание ошибки"
}
```
