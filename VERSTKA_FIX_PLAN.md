# План выявления и исправления проблем верстки

## 🎯 Цель
Выявить все неточности в верстке страницы услуги (`service_detail.html`) на stage и исправить их, приведя к эталонной верстке из `v-massaj/uslugi3.html`.

---

## 📋 Этап 1: Диагностика и выявление проблем

### 1.1. Визуальная проверка на stage
**URL:** `https://stg.formulatela58.ru/services/<id>/`

**Чеклист визуальной проверки:**

- [ ] **Grid layout формы (3x2)**
  - [ ] Форма имеет сетку 3 колонки на десктопе
  - [ ] На мобильных (< 768px) форма в 1 колонку
  - [ ] Все элементы выровнены правильно
  - [ ] Нет элементов, которые занимают всю ширину (кроме alert)

- [ ] **Элементы формы**
  - [ ] Select длительности имеет правильный стиль (`form-select`)
  - [ ] Input количества (disabled) имеет стиль `text-def`
  - [ ] Select количества имеет стиль `form-select`
  - [ ] Select мастера имеет стиль `form-select`
  - [ ] Input даты имеет стиль `form-control` и правильную ширину
  - [ ] Select времени имеет стиль `form-select` и выровнен с другими select
  - [ ] Input стоимости имеет стиль `text-def semi`

- [ ] **Размеры и отступы**
  - [ ] Все элементы имеют одинаковую высоту (40px на десктопе, 46px на мобильных)
  - [ ] Gap между элементами: 15px на десктопе, 7px на мобильных
  - [ ] Нет лишних отступов или margin

- [ ] **Отсутствующие элементы**
  - [ ] Нет label "Время записи"
  - [ ] Нет иконки часов (clock icon)
  - [ ] Нет badge с количеством слотов
  - [ ] Нет loading overlay

- [ ] **Календарь (Flatpickr)**
  - [ ] Календарь disabled когда мастер не выбран
  - [ ] Календарь имеет правильную ширину (как у select)
  - [ ] Календарь открывается при выборе мастера

### 1.2. Проверка CSS файлов

**Команды для проверки:**

```bash
# На сервере stage
cd /path/to/app
ls -lh staticfiles/css/main.css
cat staticfiles/css/main.css | grep -A 5 "\.in-form"
```

**Что проверить:**
- [ ] Файл `main.css` существует в `staticfiles/css/`
- [ ] Файл содержит стили для `.in-form`
- [ ] Стили для `.in-form` правильные:
  ```css
  .in-form {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 15px;
    margin-bottom: 20px;
  }
  ```
- [ ] Медиа-запрос для мобильных правильный:
  ```css
  @media (max-width: 768px) {
    .in-form {
      grid-template-columns: 1fr;
    }
  }
  ```
- [ ] Нет лишних `grid-column: 1 / -1` для элементов формы

### 1.3. Проверка HTML структуры

**Что проверить в шаблоне:**

- [ ] Форма имеет класс `in-form` на контейнере
- [ ] Все элементы формы находятся внутри `.in-form`
- [ ] Нет лишних оберток (`time-select-wrapper`, `time-label`, `time-badge`)
- [ ] Классы элементов правильные:
  - `form-select` для всех select
  - `form-control` для input даты
  - `text-def` для disabled input
  - `text-def semi` для стоимости

### 1.4. Проверка в DevTools браузера

**Инструкция:**

1. Открыть страницу на stage
2. F12 → Elements (Инспектор)
3. Найти форму бронирования

**Что проверить:**

- [ ] **Computed styles для `.in-form`:**
  - `display: grid`
  - `grid-template-columns: repeat(3, 1fr)` (на десктопе)
  - `gap: 15px`

- [ ] **Computed styles для элементов:**
  - Все select имеют одинаковую высоту
  - Все input имеют одинаковую высоту
  - Нет неожиданных `grid-column` правил

- [ ] **Network tab:**
  - CSS файл `main.css` загружается (статус 200)
  - Нет ошибок 404 для CSS
  - Проверить, что загружается правильная версия (не кеш)

---

## 🔧 Этап 2: Исправление проблем

### 2.1. Если CSS не обновился на stage

**Проблема:** Старые стили в `staticfiles/`

**Решение:**

```bash
# На сервере stage
cd /path/to/app/mysite
source venv/bin/activate
export DJANGO_SETTINGS_MODULE=mysite.settings.staging
python manage.py collectstatic --noinput --clear
```

**Или через GitHub Actions:**
- Уже добавлено в workflow, но можно запустить вручную через `workflow_dispatch`

### 2.2. Если grid layout не работает

**Проблема:** Элементы не в grid или неправильный grid

**Проверить в `main.css`:**

```css
/* Должно быть: */
.in-form {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 15px;
  margin-bottom: 20px;
}

/* НЕ должно быть: */
.in-form .form-control {
  grid-column: 1 / -1; /* ❌ Убрать это! */
}
.in-form .form-select {
  grid-column: 1 / -1; /* ❌ Убрать это! */
}
```

**Исправление:**
- Убедиться, что в `main.css` нет `grid-column` для элементов формы
- Только `.in-form .alert` должен иметь `grid-column: 1 / -1`

### 2.3. Если элементы не выровнены

**Проблема:** Разные высоты или стили

**Проверить в `main.css`:**

```css
/* Должно быть: */
.form-select,
.form-control {
  border-radius: 12px;
  border: 1px solid #ddd;
  font-size: 14px;
  height: 40px;
}

.text-def {
  background: #e4e4e4;
  color: #333;
  border-radius: 12px;
  height: 40px;
  line-height: 40px;
  padding: 0 15px;
  width: 100%;
  border: none;
  font-size: 14px;
}

/* На мобильных: */
@media (max-width: 767px) {
  .form-select,
  .form-control {
    height: 46px;
  }
  .text-def {
    height: 48px;
  }
}
```

### 2.4. Если есть лишние элементы

**Проблема:** Label, badge, иконки не удалены

**Проверить в шаблоне:**
- Нет `<label>` для времени
- Нет `<span id="time-badge">`
- Нет CSS для `time-select-wrapper::after` (иконка часов)

**Исправление:**
- Удалить из шаблона
- Удалить из CSS

### 2.5. Если календарь работает неправильно

**Проблема:** Календарь не disabled или неправильный размер

**Проверить JavaScript:**
- `initializeEmptyDatePicker()` не инициализирует flatpickr для disabled состояния
- Календарь включается только после выбора мастера
- Ширина календаря соответствует ширине select

---

## 🧪 Этап 3: Тестирование

### 3.1. Функциональное тестирование

- [ ] Выбор длительности обновляет количество
- [ ] Выбор количества обновляет стоимость
- [ ] Выбор мастера загружает даты
- [ ] Выбор даты загружает время
- [ ] Календарь disabled когда мастер не выбран
- [ ] Календарь открывается после выбора мастера
- [ ] Форма отправляется корректно

### 3.2. Визуальное тестирование

**Устройства для проверки:**
- [ ] Десктоп (1920x1080)
- [ ] Планшет (768px)
- [ ] Мобильный (375px)

**Браузеры:**
- [ ] Chrome
- [ ] Firefox
- [ ] Safari (если есть)
- [ ] Edge

### 3.3. Сравнение с эталоном

**Сравнить с `v-massaj/uslugi3.html`:**
- [ ] Grid layout идентичен
- [ ] Стили элементов идентичны
- [ ] Отступы и размеры идентичны
- [ ] Поведение идентично

---

## 📝 Этап 4: Документирование

### 4.1. Зафиксировать изменения

После исправления всех проблем:

```bash
git add mysite/static/css/main.css
git add mysite/website/templates/website/service_detail.html
git commit -m "Fix: исправлена верстка формы бронирования - grid 3x2, выравнивание элементов"
git push origin dev
```

### 4.2. Обновить документацию

Если нужно, обновить:
- `SERVICE_PAGE_DECOMPOSITION.md`
- `FORM_LAYOUT_FIX_PLAN.md`

---

## 🚨 Быстрые команды для диагностики

### На локальной машине:

```bash
# Проверить текущие стили в main.css
grep -A 10 "\.in-form" mysite/static/css/main.css

# Проверить структуру формы в шаблоне
grep -A 5 "in-form" mysite/website/templates/website/service_detail.html
```

### На сервере stage (через SSH):

```bash
# Проверить собранные статические файлы
ls -lh /path/to/app/staticfiles/css/main.css
cat /path/to/app/staticfiles/css/main.css | grep -A 5 "\.in-form"

# Пересобрать статику
cd /path/to/app/mysite
source venv/bin/activate
export DJANGO_SETTINGS_MODULE=mysite.settings.staging
python manage.py collectstatic --noinput --clear

# Проверить логи nginx (если используется)
sudo tail -f /var/log/nginx/error.log
```

---

## ✅ Чеклист финальной проверки

Перед закрытием задачи убедиться:

- [ ] Все визуальные проблемы исправлены
- [ ] CSS файлы обновлены на stage
- [ ] Grid layout работает правильно (3x2)
- [ ] Все элементы выровнены
- [ ] Нет лишних элементов (label, badge, иконки)
- [ ] Календарь работает корректно
- [ ] Форма функциональна на всех устройствах
- [ ] Изменения закоммичены и запушены
- [ ] Документация обновлена (если нужно)

---

## 📞 Если проблемы остаются

1. **Сделать скриншоты** проблемных мест
2. **Скопировать HTML** проблемного элемента из DevTools
3. **Скопировать Computed styles** из DevTools
4. **Проверить консоль браузера** на ошибки JavaScript
5. **Проверить Network tab** на ошибки загрузки CSS

---

**Дата создания:** 2025-12-18  
**Последнее обновление:** 2025-12-18

