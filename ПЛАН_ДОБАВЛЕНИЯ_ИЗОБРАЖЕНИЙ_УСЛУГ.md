# План добавления поля изображения для услуг

## 📋 Проблема

В верстке используются изображения для услуг, но в модели `Service` нет поля `image`. В шаблонах уже используется `service.image`, что приведет к ошибкам.

## 🎯 Цель

Добавить возможность загружать и отображать изображения для услуг через Django админку.

---

## 📝 Этап 1: Добавление поля в модель

### 1.1. Обновление модели Service

**Файл:** `mysite/services_app/models.py`

**Действия:**
1. Добавить поле `image` в модель `Service`:
   ```python
   image = models.ImageField(
       upload_to="services/",
       blank=True,
       null=True,
       verbose_name="Изображение услуги",
       help_text="Рекомендуемый размер: 800x600px"
   )
   ```

2. Параметры поля:
   - `upload_to="services/"` - изображения будут сохраняться в `media/services/`
   - `blank=True, null=True` - поле необязательное (для обратной совместимости)
   - Добавить `help_text` с рекомендациями по размеру

**Примечание:** Убедиться, что `MEDIA_ROOT` и `MEDIA_URL` настроены в `settings/base.py` (уже настроены).

---

## 📝 Этап 2: Создание миграции

### 2.1. Создание миграции

**Действия:**
1. Создать миграцию:
   ```bash
   python manage.py makemigrations services_app
   ```

2. Проверить созданную миграцию:
   - Убедиться, что поле добавлено корректно
   - Проверить, что `null=True` установлено (для существующих записей)

3. Применить миграцию:
   ```bash
   python manage.py migrate services_app
   ```

**Важно:** Миграция должна быть обратно совместимой - существующие услуги не должны сломаться.

---

## 📝 Этап 3: Обновление админки

### 3.1. Добавление поля в админку

**Файл:** `mysite/services_app/admin.py`

**Действия:**
1. Добавить `image` в `list_display` (опционально, для быстрого просмотра):
   ```python
   list_display = ("id", "name", "short", "category", "is_active", "is_popular", "image_preview")
   ```

2. Добавить метод для отображения превью изображения:
   ```python
   def image_preview(self, obj):
       if obj.image:
           return format_html('<img src="{}" style="max-height: 50px; max-width: 50px;" />', obj.image.url)
       return "—"
   image_preview.short_description = "Изображение"
   ```

3. Добавить `image` в `fields` или `fieldsets` для формы редактирования:
   ```python
   fields = ("name", "short", "description", "category", "image", "is_active", "is_popular")
   ```

4. Добавить `image` в `readonly_fields` (если нужно только просматривать):
   - НЕТ, поле должно быть редактируемым

**Примечание:** Django админка автоматически создаст виджет для загрузки изображений.

---

## 📝 Этап 4: Исправление view service_detail

### 4.1. Исправление использования options_filtered

**Файл:** `mysite/website/views.py`

**Проблема:** В шаблоне используется `service.options_filtered_count`, но в view создается `service.options_filtered` как queryset.

**Действия:**
1. Проверить текущий код view `service_detail`
2. Добавить вычисление `options_filtered_count`:
   ```python
   service.options_filtered_count = service.options_filtered.count()
   ```

3. Добавить секцию "Другие услуги":
   - Получить другие услуги из той же категории (или популярные услуги)
   - Исключить текущую услугу
   - Передать в контекст как `other_services`

**Пример кода:**
```python
# Получаем другие услуги из той же категории
if service.category:
    other_services = Service.objects.filter(
        category=service.category,
        is_active=True
    ).exclude(pk=service.id)[:8]
else:
    # Если нет категории - показываем популярные услуги
    other_services = Service.objects.filter(
        is_active=True,
        is_popular=True
    ).exclude(pk=service.id)[:8]

ctx = {
    'service': service,
    'other_services': other_services,
    # ...
}
```

---

## 📝 Этап 5: Проверка использования в шаблонах

### 5.1. Проверка всех шаблонов

**Файлы для проверки:**
- `mysite/website/templates/website/services.html` - список услуг
- `mysite/website/templates/website/service_detail.html` - детальная страница
- `mysite/website/templates/website/home.html` - главная страница (если используется)

**Действия:**
1. Убедиться, что везде используется `{% if service.image %}` перед отображением
2. Проверить fallback на статические изображения или placeholder
3. Убедиться, что пути к изображениям используют `{{ service.image.url }}`

**Текущее состояние:**
- ✅ В `services.html` уже есть проверка `{% if s.image %}`
- ✅ В `service_detail.html` уже есть проверка `{% if service.image %}`
- ✅ В `home.html` используется `cat.image` (для категорий, не услуг)

---

## 📝 Этап 6: Оптимизация изображений (опционально)

### 6.1. Добавление библиотеки для обработки изображений

**Опционально:** Можно добавить `Pillow` для автоматической оптимизации изображений при загрузке.

**Действия:**
1. Убедиться, что `Pillow` установлен (обычно уже есть для ImageField)
2. При необходимости добавить сигналы для автоматического ресайза при сохранении

**Примечание:** Это можно сделать позже, если понадобится.

---

## 📝 Этап 7: Обновление CSV импорта (опционально)

### 7.1. Поддержка изображений в CSV импорте

**Файл:** `mysite/services_app/admin.py`

**Действия:**
1. Если используется CSV импорт услуг, можно добавить поддержку путей к изображениям
2. Или оставить загрузку изображений только через админку

**Примечание:** Это опционально, так как изображения обычно загружаются вручную.

---

## 📝 Этап 8: Тестирование

### 8.1. Функциональное тестирование

**Проверить:**
- [ ] Создание новой услуги с изображением через админку
- [ ] Редактирование существующей услуги - добавление изображения
- [ ] Отображение изображения на странице списка услуг
- [ ] Отображение изображения на детальной странице услуги
- [ ] Fallback на placeholder, если изображения нет
- [ ] Адаптивность изображений (mn7/mobb7 классы)

### 8.2. Проверка миграции

**Проверить:**
- [ ] Миграция применяется без ошибок
- [ ] Существующие услуги не сломались
- [ ] Можно создать новую услугу без изображения

---

## 📋 Порядок выполнения

1. **Этап 1** - Добавить поле `image` в модель `Service`
2. **Этап 2** - Создать и применить миграцию
3. **Этап 3** - Обновить админку для загрузки изображений
4. **Этап 4** - Исправить view `service_detail` (options_filtered_count и other_services)
5. **Этап 5** - Проверить шаблоны (уже должны быть готовы)
6. **Этап 6** - Оптимизация (опционально, можно пропустить)
7. **Этап 7** - CSV импорт (опционально, можно пропустить)
8. **Этап 8** - Тестирование

---

## ⚠️ Важные замечания

1. **Обратная совместимость:** Поле должно быть `null=True, blank=True`, чтобы существующие услуги не сломались.

2. **MEDIA настройки:** Убедиться, что `MEDIA_ROOT` и `MEDIA_URL` настроены правильно (уже настроены в `settings/base.py`).

3. **Размер изображений:** Рекомендовать оптимальный размер в `help_text` (например, 800x600px).

4. **Производительность:** При большом количестве услуг можно добавить индексы или кеширование, но это не критично на первом этапе.

5. **Безопасность:** Django автоматически валидирует загружаемые файлы, но можно добавить дополнительную валидацию размера файла.

---

## 🔄 Дополнительные улучшения (на будущее)

1. **Автоматический ресайз:** Добавить сигналы для автоматического создания миниатюр
2. **Множественные изображения:** Добавить модель `ServiceImage` для галереи
3. **Оптимизация:** Автоматическая конвертация в WebP
4. **CDN:** Интеграция с облачным хранилищем для изображений

---

**Дата создания:** 2025-01-XX  
**Версия:** 1.0

