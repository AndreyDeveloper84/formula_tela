# План изменений формы бронирования на странице услуги

## 📋 Текущая ситуация

**В верстке (v-massaj/uslugi3.html):**
- 2 input с disabled: "50 минут" и "1 процедура" (длительность и количество)
- 1 select для мастера
- Дата и время
- Стоимость отдельно

**В текущем шаблоне:**
- 1 select для варианта услуги (объединяет длительность, количество и цену)
- 1 select для мастера
- Дата и время (Flatpickr)
- Стоимость в одном поле с текстом

## 🎯 Цель

1. Разделить выбор опции на **2 отдельных select**: длительность и количество
2. Сделать их динамическими (select только если есть несколько вариантов)
3. Отдельно отображать стоимость и обновлять её при изменении опций
4. Хранить все данные опций на клиенте в JavaScript

---

## 📝 Этап 1: Подготовка данных на сервере

### 1.1. Обновление view `service_detail`

**Файл:** `mysite/website/views.py`

**Действия:**
1. Создать структуру данных для передачи в шаблон:
   ```python
   # Группируем опции по уникальным длительностям и количествам
   options_data = {
       'durations': [],  # Список уникальных длительностей
       'quantities': {},  # {duration: [список количеств]}
       'options_map': {}  # {duration: {quantity: option_id}}
   }
   ```

2. Для каждой опции:
   - Извлечь уникальные `duration_min`
   - Для каждой длительности извлечь уникальные `units`
   - Создать карту: `{duration: {quantity: option_id}}`

3. Передать в контекст:
   ```python
   ctx = {
       'service': service,
       'options_data': options_data,  # Новая структура
       'options_list': options_list,  # Для JavaScript (JSON)
       # ...
   }
   ```

**Примечание:** Также передать список всех опций в JSON для JavaScript.

---

## 📝 Этап 2: Обновление HTML структуры формы

### 2.1. Замена текущего select варианта на 2 select

**Файл:** `mysite/website/templates/website/service_detail.html`

**Текущая структура:**
```html
{% if service.options_filtered_count > 1 %}
<select id="option-select">...</select>
{% elif service.options_filtered_count == 1 %}
<input type="text" disabled>
<input type="hidden" id="option-select">
{% endif %}
```

**Новая структура:**
```html
<!-- Выбор длительности -->
{% if durations_count > 1 %}
<select id="duration-select" class="form-select" required>
    <option value="">Выберите длительность</option>
    {% for duration in options_data.durations %}
    <option value="{{ duration }}">{{ duration }} минут</option>
    {% endfor %}
</select>
{% elif durations_count == 1 %}
<input type="text" class="text-def" value="{{ options_data.durations.0 }} минут" disabled>
<input type="hidden" id="duration-select" value="{{ options_data.durations.0 }}">
{% endif %}

<!-- Выбор количества -->
{% if quantities_count > 1 %}
<select id="quantity-select" class="form-select" required>
    <option value="">Выберите количество</option>
    <!-- Заполняется динамически через JavaScript -->
</select>
{% elif quantities_count == 1 %}
<input type="text" class="text-def" value="1 процедура" disabled>
<input type="hidden" id="quantity-select" value="1">
{% endif %}

<!-- Скрытое поле для ID выбранной опции -->
<input type="hidden" id="option-select" value="">
```

**Примечание:** 
- Если только одна опция - показывать disabled input
- Если несколько - показывать select
- Количество зависит от выбранной длительности (динамически через JS)

---

## 📝 Этап 3: Хранение данных на клиенте

### 3.1. Создание JavaScript объекта с данными опций

**Файл:** `mysite/website/templates/website/service_detail.html`

**В блоке `{% block extra_js %}`:**

```javascript
// Данные всех опций (передаются с сервера)
const serviceOptions = [
    {% for opt in service.options_filtered %}
    {
        id: {{ opt.id }},
        duration: {{ opt.duration_min }},
        quantity: {{ opt.units }},
        unitType: '{{ opt.unit_type }}',
        unitTypeDisplay: '{{ opt.get_unit_type_display }}',
        price: {{ opt.price|floatformat:0 }},
        yclientsId: '{{ opt.yclients_service_id|default:"" }}'
    }{% if not forloop.last %},{% endif %}
    {% endfor %}
];

// Структура для быстрого поиска опции
const optionsMap = {};
serviceOptions.forEach(opt => {
    if (!optionsMap[opt.duration]) {
        optionsMap[opt.duration] = {};
    }
    optionsMap[opt.duration][opt.quantity] = opt;
});

// Уникальные длительности
const uniqueDurations = [...new Set(serviceOptions.map(opt => opt.duration))].sort((a, b) => a - b);

// Функция получения опции по длительности и количеству
function getOptionByDurationAndQuantity(duration, quantity) {
    return optionsMap[duration] && optionsMap[duration][quantity] || null;
}
```

---

## 📝 Этап 4: Обновление логики выбора опций

### 4.1. Обработчики событий для select длительности и количества

**Действия:**
1. При изменении длительности:
   - Получить доступные количества для выбранной длительности
   - Обновить select количества
   - Если только одно количество - автоматически выбрать его
   - Обновить стоимость

2. При изменении количества:
   - Найти опцию по длительности и количеству
   - Обновить скрытое поле `option-select`
   - Обновить стоимость
   - Загрузить мастеров (если нужно)

3. Функция обновления стоимости:
   ```javascript
   function updatePrice() {
       const duration = document.getElementById('duration-select').value;
       const quantity = document.getElementById('quantity-select').value;
       
       if (duration && quantity) {
           const option = getOptionByDurationAndQuantity(parseInt(duration), parseInt(quantity));
           if (option) {
               const priceDisplay = document.getElementById('price-display');
               priceDisplay.value = `Стоимость: ${formatPrice(option.price)} ₽`;
               
               // Обновить скрытое поле
               document.getElementById('option-select').value = option.id;
               
               // Загрузить мастеров
               loadMasters();
           }
       } else {
           document.getElementById('price-display').value = 'Стоимость: — ₽';
       }
   }
   ```

---

## 📝 Этап 5: Обновление select мастера

### 5.1. Форматирование имени мастера

**Текущая проблема:** В верстке показывается "Фамилия Имя", а сейчас может быть просто имя.

**Действия:**
1. Проверить формат данных мастера в API
2. Обновить отображение в select:
   ```javascript
   // В функции loadMasters()
   option.textContent = `${master.last_name || ''} ${master.first_name || master.name}`.trim();
   ```

**Примечание:** Если в модели Master нет полей `first_name` и `last_name`, использовать поле `name` как есть.

---

## 📝 Этап 6: Обновление отображения стоимости

### 6.1. Отдельное поле для стоимости

**Текущее:**
```html
<input type="text" id="price-display" class="text-def semi" value="Стоимость: — ₽" disabled>
```

**Новое:** Оставить как есть, но обновлять через JavaScript при изменении опций.

**Форматирование цены:**
```javascript
function formatPrice(price) {
    return new Intl.NumberFormat('ru-RU').format(price);
}
```

---

## 📝 Этап 7: Сохранение обратной совместимости

### 7.1. Поддержка старого способа выбора (если только одна опция)

**Действия:**
1. Если `service.options_filtered_count == 1`:
   - Показать disabled input для длительности и количества
   - Автоматически установить скрытое поле `option-select`
   - Показать стоимость сразу

2. Если `service.options_filtered_count == 0`:
   - Показать предупреждение (как сейчас)

---

## 📝 Этап 8: Обновление функции loadMasters

### 8.1. Получение ID опции из скрытого поля

**Текущий код:**
```javascript
const optionElem = document.getElementById('option-select');
if (optionElem.tagName === 'SELECT') {
    serviceOptionId = optionElem.value;
} else {
    serviceOptionId = optionElem.value;
}
```

**Новый код:**
```javascript
const optionElem = document.getElementById('option-select');
serviceOptionId = optionElem.value; // Всегда hidden input
```

---

## 📝 Этап 9: Обновление функции отправки формы

### 9.1. Проверка выбранных опций

**Действия:**
1. Перед отправкой проверить:
   - Выбрана ли длительность
   - Выбрано ли количество
   - Есть ли ID опции в скрытом поле

2. Обновить `bookingData` с правильными данными

---

## 📋 Порядок выполнения

1. **Этап 1** - Подготовка данных на сервере (view)
2. **Этап 2** - Обновление HTML структуры формы
3. **Этап 3** - Создание JavaScript объекта с данными
4. **Этап 4** - Обработчики событий для select
5. **Этап 5** - Обновление select мастера (формат имени)
6. **Этап 6** - Обновление отображения стоимости
7. **Этап 7** - Обратная совместимость
8. **Этап 8** - Обновление loadMasters
9. **Этап 9** - Обновление отправки формы

---

## ⚠️ Важные замечания

1. **Обратная совместимость:** Сохранить работу формы для случая с одной опцией
2. **Валидация:** Проверять выбор длительности и количества перед отправкой
3. **UX:** Автоматически выбирать единственное значение, если оно одно
4. **Производительность:** Данные опций хранятся на клиенте, не нужно делать запросы при изменении
5. **Форматирование:** Использовать правильное форматирование цены (разделители тысяч)

---

## 🔄 Дополнительные улучшения

1. **Визуальная обратная связь:** Показывать загрузку при обновлении стоимости
2. **Кеширование:** Кешировать загруженных мастеров для выбранной опции
3. **Валидация:** Добавить визуальную индикацию невалидных полей

---

**Дата создания:** 2025-01-XX  
**Версия:** 1.0

